import Client from 'connectapi_core';
import Demo from 'connectapi_demo';


Client.set_url("https://example.com")
Client.set_token("<TOKEN>")

let service = Demo(client = new Client());

let result = service.random_int_from_range(a=42, b=42, );
console.log(result);